# Cap browser uploads. In the split deployment the dashboard runs on the LAPTOP
# (not the constrained Jetson), so this can be generous. The dashboard also rejects
# files over this size in code. Picked up when Streamlit runs with this working dir.
[server]
maxUploadSize = 200
